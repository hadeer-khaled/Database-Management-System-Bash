
"${actions[@]}"

[[:space:]]

${#string} --> to get the length of string

if ! [[ $DB_Name =~ ^[a-zA-Z][a-zA-Z0-9_]*$ ]] --- > =~  not supported within []
					       --- > ~ syntax error near `~'


